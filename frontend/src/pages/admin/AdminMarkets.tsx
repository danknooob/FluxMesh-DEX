export function AdminMarkets() {
  return (
    <div>
      <h1>MCP — Config</h1>
      <p>Markets, risk limits, feature flags. Desired state published to <code>control.config</code>.</p>
    </div>
  );
}
