# API-specific lint config; runs from api/ in CI.
# See https://golangci-lint.run/docs/product/migration-guide for v2 config.
version: 2

run:
  timeout: 5m

linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - ineffassign
